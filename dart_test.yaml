# The golden tests are a preview tool, not a regression gate: font
# rasterisation differs between platforms, so they would fail on any machine
# other than the one that generated them. Skipped by default.
#
#   flutter test --run-skipped --update-goldens test/golden_test.dart
#
tags:
  golden:
    skip: "preview renders - run explicitly with --run-skipped"
  economy:
    skip: "economy instrument - run explicitly with --run-skipped"
